#include <stdio.h>
#include "main.h"

/**
 * _printf: prints output
 * @format: the input format
 * Return: 0
 */

int _printf(const char *format, ...)
{
	/**
	 * we try to put out the charachters
	 */

	_putchar(...);
	return (0);
}
